{
  "logging": {
    "IncludeScopes": true,
    "logLevel": {
      "default": "Information"
    },
    "logger": "fluentd"
  },

  "allowedHosts": "*",

  "bus": {
    "receiveEndpoint": "ordering",
    "host": "localhost",
    "username": "guest",
    "password": "guest"
  },
  
  "application": {
    "portNumber": 7003
  },
  
  "database": {
    "connectionString": "host=localhost;database=Ordering;password=ABC123!!;username=application"
  }
}