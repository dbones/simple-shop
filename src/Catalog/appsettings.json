{
  "logging": {
    "IncludeScopes": true,
    "logLevel": {
      "default": "Information"
    },
    "logger": "fluentd"
  },

  "allowedHosts": "*",

  "bus": {
    "receiveEndpoint": "catalog",
    "host": "localhost",
    "username": "guest",
    "password": "guest"
  },
  
  "application": {
    "portNumber": 7002
  },
  
  "database": {
    "connectionString": "host=localhost;database=Catalog;password=ABC123!!;username=application"
  }
}